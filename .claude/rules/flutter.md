---
paths:
  - "lib/**/*.dart"
  - "test/**/*.dart"
  - "pubspec.yaml"
---

# Flutter rules

- Preserve null safety and existing architecture.
- Prefer small widgets and explicit loading, empty, error, and success states.
- Keep UI accessible and responsive.
- Use `flutter analyze` and `flutter test` after behavior changes.
- Ask before adding or upgrading packages.
